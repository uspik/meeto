# Версии закреплены точно — сборка должна быть воспроизводимой.
#
# Осторожно с pydantic: aiogram держит на него верхнюю границу, поэтому
# менять их версии нужно вместе. Именно на этом ломалась сборка раньше:
# aiogram 3.15 требует pydantic <2.10, а в файле стояло 2.10.4.
#
# Проверить совместимость после правки:
#   pip install --dry-run -r requirements.txt

fastapi==0.115.14
uvicorn[standard]==0.34.3
sqlalchemy[asyncio]==2.0.51
asyncpg==0.30.0
aiosqlite==0.21.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
aiogram==3.30.0
redis==5.3.1
python-multipart==0.0.32
